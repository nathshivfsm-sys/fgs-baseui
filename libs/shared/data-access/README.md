# `@cms/shared-data-access`

TanStack Query options factories for platform services shared across remotes.

Wire DTOs live in `@cms/shared-contract`. File Service Attachment is the first
module; clone `src/lib/attachment/` (and Tax in settings) for the next one.

{
  "name": "settings-contract",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/settings/contract/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:settings", "type:contract"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "cache": true,
      "dependsOn": ["^build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/settings/contract",
        "main": "libs/settings/contract/src/index.ts",
        "tsConfig": "libs/settings/contract/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "tsc --noEmit -p libs/settings/contract/tsconfig.json"
      }
    }
  }
}
